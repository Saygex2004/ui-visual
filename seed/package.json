{
  "name": "@pvp/seed",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./lib.ts",
  "types": "./lib.ts",
  "exports": {
    ".": "./lib.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "echo \"(no unit tests in seed/)\" && exit 0",
    "test:integration": "echo \"(seed.ts is exercised by apps/server's integration suite)\" && exit 0",
    "build": "echo \"(seed/ ships as source, run via tsx)\" && exit 0"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3"
  }
}
