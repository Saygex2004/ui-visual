// @pvp/shared — Zod schemas + pure domain rules, imported by both apps.
// Phase 0 stub: real schemas (schemas/), domain rules (domain/) and constants
// (constants/) arrive in Phase 1. This package has zero runtime dependencies
// beyond Zod and performs no I/O.

export * from './constants/index.js';
